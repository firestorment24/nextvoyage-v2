'use client';

import React from 'react';

const Navigation = () => (  
  <nav className="fixed top-0 w-full z-50 px-12 py-8 flex justify-between items-center bg-gradient-to-b from-black to-transparent font-sans">  
    <div className="text-amber-100/90 tracking-[0.5em] uppercase text-xs font-bold">  
      <a href="/">NexVoyage Collective</a>  
    </div>  
    <div className="flex gap-12 text-stone-500 uppercase tracking-[0.3em] text-[10px]">  
      <a href="/sanctuaries" className="text-amber-100 transition-colors">Sanctuaries</a>  
      <a href="/reserve" className="hover:text-amber-100 transition-colors">Reserve</a>  
    </div>  
  </nav>  
);

const Footer = () => (  
  <footer className="bg-black py-20 px-12 border-t border-stone-900 font-sans">  
    <div className="max-w-6xl mx-auto flex justify-between items-center text-[10px] text-stone-600 uppercase tracking-[0.4em]">  
      <p>© 2026 NexVoyage</p>  
      <p>By Appointment Only</p>  
    </div>  
  </footer>  
);

const sanctuaries = [  
  {  
    title: "The Maybourne Riviera",  
    location: "Roquebrune-Cap-Martin, France",  
    image: "https://cdn.marblism.com/QaCq5-bi5FE.webp",  
    link: "/sanctuaries/maybourne",  
    tagline: "Architectural Cliffside Living"  
  },  
  {  
    title: "RC Yacht Collection",  
    location: "Global Charter",  
    image: "https://cdn.marblism.com/bezdT4Fs3Bm.webp",  
    link: "/sanctuaries/rc-yacht",  
    tagline: "Absolute Isolation at Sea"  
  },  
  {  
    title: "TCI Trifecta",  
    location: "Grace Bay, Turks & Caicos",  
    image: "https://cdn.marblism.com/cUyJDkxI7Ky.webp",  
    link: "/sanctuaries/tci-trifecta",  
    tagline: "The Sound of Gravity"  
  }  
];

export default function SanctuariesIndex() {  
  return (  
    <main className="bg-black text-stone-200 min-h-screen font-serif selection:bg-amber-200/20">  
      <Navigation />

      {/* Header Section */}  
      <section className="pt-40 pb-20 px-12 max-w-7xl mx-auto">  
        <h2 className="text-amber-100/40 uppercase tracking-[0.5em] text-[10px] mb-6 font-sans">  
          The 2026 Collection  
        </h2>  
        <h1 className="text-5xl md:text-7xl font-light italic text-stone-100 tracking-tight">  
          Curated <span className="text-stone-500 font-serif not-italic">Sanctuaries</span>  
        </h1>  
      </section>

      {/* Gallery Grid */}  
      <section className="px-6 pb-40 grid grid-cols-1 md:grid-cols-3 gap-6">  
        {sanctuaries.map((s, i) => (  
          <a key={i} href={s.link} className="group block relative aspect-[3/4] overflow-hidden bg-stone-900">  
            {/* Background Image */}  
            <img   
              src={s.image}   
              alt={s.title}   
              className="absolute inset-0 w-full h-full object-cover opacity-60 grayscale-[40%] group-hover:scale-105 group-hover:grayscale-0 transition-all duration-1000"  
            />  
              
            {/* Vignette Overlays */}  
            <div className="absolute inset-0 bg-gradient-to-t from-black via-transparent to-transparent opacity-80"></div>  
              
            {/* Content Overlay */}  
            <div className="absolute inset-0 p-12 flex flex-col justify-end transform translate-y-4 group-hover:translate-y-0 transition-transform duration-700">  
              <span className="text-amber-200/50 text-[10px] uppercase tracking-[0.4em] font-sans mb-2">  
                {s.location}  
              </span>  
              <h3 className="text-3xl text-stone-100 mb-4 font-light tracking-tight group-hover:text-amber-100 transition-colors">  
                {s.title}  
              </h3>  
              <p className="text-stone-400 text-sm italic font-serif opacity-0 group-hover:opacity-100 transition-opacity duration-700 delay-100">  
                {s.tagline}  
              </p>  
                
              <div className="mt-8 w-0 group-hover:w-full h-px bg-amber-200/20 transition-all duration-1000"></div>  
            </div>  
          </a>  
        ))}  
      </section>

      <Footer />  
    </main>  
  );  
}  

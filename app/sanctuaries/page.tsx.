'use client'

import React from 'react'

const destinations = [  
  {  
    title: "The Maybourne Riviera",  
    location: "Roquebrune-Cap-Martin, France",  
    description: "Suspended between sky and sea. The ultimate Mediterranean vantage point for those who require total discretion.",  
    image: "/maybourne-hero.jpg" // Placeholder path  
  },  
  {  
    title: "RC Yacht Collection",  
    location: "Global Itineraries",  
    description: "The gold standard of the Ritz-Carlton, reimagined for the open ocean. A private club atmosphere with no fixed address.",  
    image: "/rc-yacht.jpg"  
  },  
  {  
    title: "The TCI Trifecta",  
    location: "Turks & Caicos",  
    description: "Amanyara, Como Parrot Cay, and Wymara. Three distinct expressions of Caribbean isolation.",  
    image: "/tci.jpg"  
  }  
]

export default function SanctuariesPage() {  
  return (  
    <div className="min-h-screen bg-black text-zinc-100 font-light selection:bg-[#B5A642]">  
      {/* Minimal Header */}  
      <nav className="p-10 flex justify-between items-end border-b border-zinc-900">  
        <div>  
          <h1 className="text-2xl tracking-[0.2em] uppercase font-medium">The Collection</h1>  
          <p className="text-[10px] tracking-[0.3em] uppercase opacity-40 mt-2">NexVoyage Collective / 2026 Inventory</p>  
        </div>  
        <a href="/reserve" className="text-[10px] tracking-[0.3em] uppercase border border-zinc-800 px-6 py-3 hover:border-zinc-100 transition-all">  
          Inquire  
        </a>  
      </nav>

      <main className="py-20">  
        <div className="grid grid-cols-1 gap-32 px-10">  
          {destinations.map((dest, index) => (  
            <section key={dest.title} className="group cursor-pointer max-w-6xl mx-auto w-full">  
              <div className="grid grid-cols-1 md:grid-cols-12 gap-12 items-center">  
                  
                {/* Text Content */}  
                <div className="md:col-span-5 space-y-8 order-2 md:order-1">  
                  <span className="text-[#B5A642] text-[10px] tracking-[0.5em] uppercase font-semibold">  
                    0{index + 1} // {dest.location}  
                  </span>  
                  <h2 className="text-5xl md:text-7xl font-extralight tracking-tighter leading-none italic">  
                    {dest.title}  
                  </h2>  
                  <p className="text-zinc-400 text-sm leading-relaxed max-w-sm font-light">  
                    {dest.description}  
                  </p>  
                  <div className="pt-4">  
                    <button className="text-[10px] tracking-[0.4em] uppercase border-b border-zinc-800 pb-2 group-hover:border-[#B5A642] transition-all">  
                      Explore Sanctuary  
                    </button>  
                  </div>  
                </div>

                {/* Visual Placeholder */}  
                <div className="md:col-span-7 aspect-[16/10] bg-zinc-900 overflow-hidden order-1 md:order-2 relative">  
                   <div className="absolute inset-0 bg-gradient-to-t from-black/60 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-700" />  
                   <div className="w-full h-full border border-zinc-800 flex items-center justify-center text-[10px] tracking-[0.5em] uppercase opacity-20 group-hover:opacity-40 transition-all">  
                      [ High Fidelity Visual ]  
                   </div>  
                </div>

              </div>  
            </section>  
          ))}  
        </div>  
      </main>

      <footer className="p-20 text-center border-t border-zinc-900 mt-20">  
        <p className="text-[10px] tracking-[0.5em] uppercase opacity-30">  
          Access to the collection is strictly by referral or application.  
        </p>  
      </footer>  
    </div>  
  )  
}  
